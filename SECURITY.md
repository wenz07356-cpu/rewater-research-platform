# 安全策略

## 报告安全问题

请不要通过公开 Issue 报告漏洞、密钥泄露、内部地址或业务数据暴露问题。
请改用仓库所有者指定的私密联系方式，并提供：

- 受影响的服务、版本或提交；
- 复现步骤与潜在影响；
- 已采取的临时缓解措施；
- 可安全共享的日志或截图。

在维护者确认修复并完成必要的凭据轮换前，请勿公开披露细节。

## 敏感信息规则

- 真实 `.env`、API Key、Token、证书和生产账号不得提交到 Git；
- GitHub Actions 密钥应保存在 Repository 或 Environment Secrets 中；
- 示例配置只能包含空值、明显占位值或明确标注的本地开发默认值；
- 一旦凭据进入提交历史，应先撤销或轮换凭据，再处理 Git 历史。
